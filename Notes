1. The scanner / lexer converts "-1" to a number token, so "(x - 1)" is a valid
expression but "(x-1)" is not
2. "keys" means "Object.keys" and this function is intercepted so as to present
the illusion of not executing in a sandbox
